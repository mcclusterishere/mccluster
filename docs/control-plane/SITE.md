# Live website

The live site is `https://matthew.mccluster.org`.

It ships from **this repo**: `mcclusterishere/mccluster`.

`mcclusterishere/Here` is the old website repo. Do not deploy the live site from Here.

| Thing | Source |
| --- | --- |
| Public pages | `mcclusterishere/mccluster` |
| API Worker | Cloudflare Worker `mccluster` (`workers/mccluster`) |
| API URL | `https://api.mccluster.org` |
| Data | Supabase `zmnhbrjyhxzhkxmhkexs` |

Cloudflare Git integration and GitHub Pages for `matthew.mccluster.org` must point at **mccluster**, not Here.
