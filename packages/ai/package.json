{
  "name": "@here/ai",
  "private": true,
  "version": "0.1.0",
  "description": "The vendor-neutral AI gateway. No component outside this package may call a model vendor. Server-side only — no key in this package ever reaches a client.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
