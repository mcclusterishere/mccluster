{
  "name": "@here/commerce",
  "private": true,
  "version": "0.1.0",
  "description": "Payment and fulfillment contracts — the order system is modeled independently of any one checkout or fulfillment provider.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
