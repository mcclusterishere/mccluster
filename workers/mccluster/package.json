{
  "name": "mccluster",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker mccluster. The only Worker.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  }
}
