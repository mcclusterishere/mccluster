{
  "name": "mccluster",
  "private": true,
  "version": "1.0.0",
  "description": "HERE. A McCluster Production. The native shell around the platform.",
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0",
    "docx": "^9.7.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  },
  "scripts": {
    "test": "node --test-reporter=spec --test supabase/tests/*.test.mjs || for f in supabase/tests/*.test.mjs; do echo \"\\n== $f\"; node \"$f\" || exit 1; done",
    "test:social": "node supabase/tests/social-authz.test.mjs"
  }
}
