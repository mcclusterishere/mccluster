alter function public.compute_credits_for_microusd(bigint)
  set search_path = pg_catalog, public;

alter function public.compute_retail_microusd(bigint, integer)
  set search_path = pg_catalog, public;

alter function public.normalize_mccluster_id(text)
  set search_path = pg_catalog, public;
