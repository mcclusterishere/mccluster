{
  "name": "@mccluster/legacy-api-archive",
  "private": true,
  "version": "0.1.0",
  "description": "Archived predecessor to the canonical Cloudflare Worker at workers/mccluster. Do not deploy or run as a backend.",
  "type": "module",
  "scripts": {
    "dev": "node -e \"console.error('DISABLED: use workers/mccluster'); process.exit(1)\"",
    "start": "node -e \"console.error('DISABLED: canonical backend is workers/mccluster'); process.exit(1)\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "fastify": "^4.28.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
