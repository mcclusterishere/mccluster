# This credential protects the loopback-only OpenCode server. Generate a random
# value on the host and copy the SAME value into core.env. It is not a Supabase
# or GitHub credential.
OPENCODE_SERVER_USERNAME=opencode
OPENCODE_SERVER_PASSWORD=
